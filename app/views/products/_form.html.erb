<div class="container">
<%= form_with(model: product, local: true) do |form| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <%= form.text_field :name ,class: "form-control", placeholder: "Enter product name"%>
<br>
  <div class="field">
    <p><%= form.number_field :price ,class: "form-control", placeholder: "Enter price Rs"%></p> 
  </div>

  <div class="field">
    <%= form.label :Menu %>
    <%= form.select(:one_menu_id, OneMenu.all.collect {|p| [ p.name, p.id ] }, { include_blank: true }) %>
  </div>
  <div class="actions">
    <%= form.submit class: "btn btn-success" %>
  </div>
<% end %>
</div>