<p class="pull-right">
<% if user_signed_in? %>
  Logged in as <strong><%= current_user.email %></strong>.
  <%= link_to 'Edit profile', edit_user_registration_path, :class => 'navbar-link btn btn-success btn-sm',style:'color: white' %> |
  <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'navbar-link btn btn-danger btn-sm', style:'color: white'  %>
<% else %>
  <%= link_to "Sign up", new_user_registration_path, :class => 'navbar-link', class: 'btn btn-success btn-sm'  %> |
  <%= link_to "Login", new_user_session_path, :class => 'navbar-link'  %>
<% end %>
</p>
	<% if !current_user.sale? || !current_user.admin? %>
		<%= link_to 'My order status', status_orders_path, class: "btn btn-warning", style: "color: white;"%>
	<%end%>
 <% if current_user.sale? || current_user.admin? %>
	 <%= link_to 'add new menu', new_one_menu_path, class: "btn btn-warning", style: "color: white;"%>

	 	<a type="button" style="color: white;" class="btn btn-primary" href="<%= inbox_orders_path%>">
  			Inbox <span class="badge badge-light"><%=Order.placed.count%></span>
		</a>
	 <%= link_to 'add new product', new_product_path, class: "btn btn-warning", style: "color: white;"%>
	  <%= link_to 'Expenses', expenses_path, class: "btn btn-warning", style: "color: white;"%>
	 <% if current_user.admin? %>
	 	<%= link_to 'History', orders_path, class: "btn btn-warning", style: "color: white;" %>

	 	<a type="button" style="color: white;" class="btn btn-success" href="<%= all_users_orders_path%>">
  			Total users <span class="badge badge-light"><%=User.count%></span>
	 	<%= link_to 'All products', products_path, class: "btn btn-warning", style: "color: white;" %>
	 <%end%>
 <%end%>
<div class="container">
<h1>New Order</h1>
</div>

<%= render 'form', order: @order %>


