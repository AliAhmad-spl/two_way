<%= form_with(model: one_menu, local: true) do |form| %>
  <% if one_menu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(one_menu.errors.count, "error") %> prohibited this one_menu from being saved:</h2>

      <ul>
      <% one_menu.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
